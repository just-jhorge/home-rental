const Footer = () => {
	return (
		<footer className="w-full flex items-center justify-center py-1.5 border-t border-gray-300">
			{new Date().getFullYear()} &copy; George Inc
		</footer>
	);
};

export default Footer;
